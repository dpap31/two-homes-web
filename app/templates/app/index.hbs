<div class='container'>
  <div class='col-md-2'>
		{{#if session.isAuthenticated}}
		<div class="panel panel-default">
		  <!-- Default panel contents -->
		  <div class="panel-heading">
		    Conversations
		  </div>
      <div class="list-group">
        {{#each sessionUser.user.conversations as |convo|}}
          {{#link-to 'app.conversations.conversation' convo}}
           <a class="list-group-item">{{convo.code}}</a>
          {{/link-to}}
        {{/each}}
      </div>
		</div>
		{{/if}}
		{{#link-to 'app.conversations.new'}}
      <a class="btn btn-success"role="button">New Conversation</a>
    {{/link-to}}
	</div>
	<div class='col-md-10'>
	  {{outlet}}
	  <div class="well">MESSAGE THREAD GOES HERE!</div>
	  <div class="well">MESSAGE THREAD GOES HERE!</div>
	</div>
</div>
