<div class="chatscreen">
  <div class="md-padding chatscreen-messages">
  {{#paper-card}}
    {{#paper-card-content}}
      {{#paper-list class="conversation"}}
        {{#each model as |message|}}
          {{#if message.isSent}}
            {{#if (eq message.user.id sessionUser.user.id) }}
              {{#paper-item class="md-3-line self"}}
                <div class='message'>
                  <div class="md-list-item-text">
                    <h3>{{message.body}}</h3>
                    <p>
                      <time datetime='{{message.createdAt}}'>{{message.user.firstName}} • {{message.createdAt}}</time>
                      Message Convo ID: {{message.coversation.id}}
                      Message UID: {{message.user.id}}
                      Session UID:{{sessionUser.user.id}}
                    </p>
                  </div>
                </div>
                <img src='http://placehold.it/50/98fb98/ffffff&text={{message.user.initials}}' class="md-avatar avatar" />
              {{/paper-item}}
              {{else}}
              {{#paper-item class="md-3-line other"}}
                <img src='http://placehold.it/50/55C1E7/fff&text={{message.user.initials}}' class="md-avatar avatar" />
                <div class="message">
                  <div class="md-list-item-text">
                    <h3>{{message.body}}</h3>
                    <p>
                      <time datetime='{{message.createdAt}}'>{{message.user.firstName}} • {{message.createdAt}}</time>
                      Message UID: {{message.user.id}}
                      Session UID:{{sessionUser.user.id}}
                    </p>
                  </div>
                </div>
              {{/paper-item}}
            {{/if}}
          {{/if}}
          {{else}}
            <h3>No messages yet.</h3>
        {{/each}}
      {{/paper-list}}
    {{/paper-card-content}}
  {{/paper-card}}
  </div>
</div>
{{!-- <div layout='flex' class='md-padding'>
  <div flex=1>{{paper-input label='Message' value=model.newMessage.body}}</div>
  <div>{{#paper-button action='sendMessage' disabled=isDisabled raised=true classNames='md-primary'}}Send{{/paper-button}}</div>
</div> --}}
{{outlet}}

{{!-- <div class='chat-panel col-md-10 col-md-offset-1'>
  {{chat-header conversation=model}}
  {{chat-participants participants=model.users}}
</div>
<div class='messages-region col-md-10 col-md-offset-1'>
  {{message-region messages=model.messages}}
  {{outlet}}
</div> --}}


{{!-- <div class='form-group'>
    {{textarea value=model.newMessage.body insert-newline='sendMessageOnEnter' class='form-control' rows='4' id='message-text' placeholder='Write your message..'}}
  </div>
  <button class='btn btn-primary send-btn'{{action 'sendMessage' model.newMessage}} disabled={{isDisabled}}>{{fa-icon 'paper-plane'}}   Send</button>
{{outlet}}
 --}}


