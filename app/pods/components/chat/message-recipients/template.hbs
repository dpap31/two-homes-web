<h3>{{title}}</h3>
<ul>
  {{#each conversation.users as |participant| }}
    {{#unless (eq participant.id sessionUser.user.id)}}
      <li id='participant'>{{ participant.fullName }}</li>
    {{/unless}}
  {{/each}}
</ul>

{{yield}}
