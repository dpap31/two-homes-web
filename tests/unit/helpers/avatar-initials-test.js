import { avatarInitials } from 'two-homes-web/helpers/avatar-initials';
import { module, test } from 'qunit';

module('Unit | Helper | avatar initials');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = avatarInitials([42]);
  assert.ok(result);
});
