import { arrayDiff } from 'two-homes-web/helpers/array-diff';
import { module, test } from 'qunit';

module('Unit | Helper | array diff');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayDiff([42]);
  assert.ok(result);
});
