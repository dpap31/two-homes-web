import { fullName } from 'two-homes-web/helpers/full-name';
import { module, test } from 'qunit';

module('Unit | Helper | full name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fullName([42]);
  assert.ok(result);
});
